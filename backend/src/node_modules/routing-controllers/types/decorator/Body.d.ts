import { BodyOptions } from 'routing-controllers/types/decorator-options/BodyOptions';
/**
 * Allows to inject a request body value to the controller action parameter.
 * Must be applied on a controller action parameter.
 */
export declare function Body(options?: BodyOptions): Function;
